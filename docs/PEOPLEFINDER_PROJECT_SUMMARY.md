# PeopleFinder - Итоговая документация проекта

## Обзор

**PeopleFinder** - веб-приложение для OSINT поиска людей по фотографии или текстовым данным (username/email).

## Созданные файлы

### Backend (Python/FastAPI)

```
backend/
├── main.py                      # Главный файл FastAPI приложения
├── config.py                    # Конфигурация приложения
└── modules/
    ├── __init__.py              # Инициализация модуля
    ├── sherlock_search.py       # Поиск по username/email (логика Sherlock)
    └── image_search.py          # Reverse image search (Google/Yandex/TinEye)
```

### Frontend (HTML/CSS/JS)

```
frontend/
├── index.html                   # Главная страница (минималистичный UI)
└── js/
    └── app.js                   # JavaScript логика приложения
```

### Конфигурация

```
├── requirements.txt             # Python зависимости
├── .env.example                 # Пример конфигурации
└── PeopleFinder.gitignore       # Git ignore файл
```

### Документация

```
├── PEOPLEFINDER_README.md       # Полная документация
├── QUICKSTART.md                # Быстрый старт
└── PEOPLEFINDER_PROJECT_SUMMARY.md  # Этот файл
```

## Функциональность

### 1. Поиск по Username/Email
- **Файл**: `backend/modules/sherlock_search.py`
- **Платформы**: GitHub, Instagram, Twitter, Reddit, YouTube, TikTok, Facebook, LinkedIn, Telegram, VK, Medium, Twitch, Pinterest, Tumblr, Snapchat
- **Технология**: Асинхронные HTTP запросы через aiohttp
- **Метод**: Проверка существования профилей по HTTP статус-кодам

### 2. Поиск по Фотографии
- **Файл**: `backend/modules/image_search.py`
- **Возможности**:
  - Reverse Image Search (Google Images, Yandex Images, TinEye)
  - Распознавание лиц (face_recognition + dlib)
  - Извлечение и кодирование лиц
  - Сравнение лиц между изображениями
- **Формат**: JPG, JPEG, PNG, GIF, WEBP (до 10MB)

### 3. Backend API (FastAPI)
- **Файл**: `backend/main.py`
- **Endpoints**:
  - `GET /` - Главная страница
  - `GET /api/health` - Проверка здоровья
  - `POST /api/search/text` - Поиск по тексту
  - `POST /api/search/image` - Поиск по изображению
  - `POST /api/search/combined` - Комбинированный поиск
  - `DELETE /api/cleanup` - Очистка загрузок
- **Документация**: `/docs` (Swagger), `/redoc` (ReDoc)

### 4. Frontend Interface
- **Файл**: `frontend/index.html`
- **Дизайн**: Минималистичный стиль Google/Яндекс
- **Фичи**:
  - Переключатель "Поиск по фото" / "Поиск по данным"
  - Drag & Drop для изображений
  - Превью загруженного изображения
  - Карточки результатов с уровнем уверенности
  - Адаптивный дизайн (Tailwind CSS)
  - Анимации и transitions

## Технический стек

### Backend
- **FastAPI** - веб-фреймворк
- **face_recognition** + **dlib** - распознавание лиц
- **aiohttp** - асинхронные HTTP клиент
- **BeautifulSoup** - парсинг HTML
- **Pillow** - обработка изображений
- **pydantic** - валидация данных
- **uvicorn** - ASGI сервер

### Frontend
- **HTML5/CSS3** - современная разметка
- **Tailwind CSS** - утилитарный CSS
- **Vanilla JavaScript** - без фреймворков
- **Fetch API** - HTTP клиент

## Быстрый запуск

```bash
# 1. Установка зависимостей
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 2. Настройка
cp .env.example .env

# 3. Запуск
cd backend
python main.py

# 4. Откройте браузер
open http://localhost:8000
```

## API Примеры

### Поиск по username
```bash
curl -X POST http://localhost:8000/api/search/text \
  -H "Content-Type: application/json" \
  -d '{"query": "github", "search_type": "username", "max_sites": 15}'
```

### Поиск по изображению
```bash
curl -X POST http://localhost:8000/api/search/image \
  -F "file=@photo.jpg"
```

## Архитектура

```
┌─────────────────┐
│   Browser UI    │
│  (Tailwind CSS) │
└────────┬────────┘
         │ HTTP/Fetch API
         ▼
┌─────────────────┐
│  FastAPI Server │
│   (main.py)     │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────┐  ┌──────┐
│Search│  │Image │
│Module│  │Module│
└──┬───┘  └──┬───┘
   │         │
   ▼         ▼
┌──────────────┐
│External APIs │
│Google/Yandex │
│ HIBP/Social  │
└──────────────┘
```

## Безопасность

### Реализовано:
- ✅ Валидация типов файлов
- ✅ Ограничение размера загрузок (10MB)
- ✅ CORS защита
- ✅ Автоматическое удаление загруженных файлов
- ✅ Escape HTML в результатах
- ✅ Rate limiting ready (через config)

### Рекомендации:
- 🔒 Используйте HTTPS в production
- 🔒 Добавьте аутентификацию для публичного доступа
- 🔒 Настройте firewall rules
- 🔒 Регулярно обновляйте зависимости

## Ограничения и известные проблемы

1. **Reverse Image Search**:
   - Google/Yandex могут блокировать скрапинг
   - Cloudflare/CAPTCHA защита на некоторых сайтах
   - Рекомендуется использовать официальные API для production

2. **Username Search**:
   - Некоторые сайты требуют авторизацию
   - Rate limiting со стороны платформ
   - Изменение структуры страниц может сломать парсинг

3. **Face Recognition**:
   - Требует значительные ресурсы CPU/RAM
   - dlib установка может быть сложной на Windows
   - Точность зависит от качества изображений

## Улучшения для Production

### Обязательно:
- [ ] Docker контейнеризация
- [ ] Добавить аутентификацию (JWT)
- [ ] Настроить логирование (structlog)
- [ ] Добавить мониторинг (Prometheus)
- [ ] Кеширование результатов (Redis)
- [ ] Async database (PostgreSQL)

### Желательно:
- [ ] Интеграция с официальными API
- [ ] Machine Learning для улучшения точности
- [ ] История поисков
- [ ] Экспорт результатов (JSON/CSV/PDF)
- [ ] Admin панель
- [ ] Rate limiting middleware
- [ ] CI/CD pipeline

## Лицензия и Ethics

**⚠️ ВАЖНО**: Этот инструмент предназначен ТОЛЬКО для:
- Легальных OSINT исследований
- Журналистских расследований
- Кибербезопасности
- Поиска пропавших людей
- Образовательных целей

**ЗАПРЕЩЕНО** использовать для:
- ❌ Преследования (stalking)
- ❌ Харассмента
- ❌ Нарушения приватности
- ❌ Мошенничества
- ❌ Любых незаконных действий

## Контакты и поддержка

- **Документация**: См. PEOPLEFINDER_README.md
- **Быстрый старт**: См. QUICKSTART.md
- **Issues**: GitHub Issues (если опубликовано)

---

**Проект готов к использованию!**

Для запуска выполните:
```bash
cd backend && python main.py
```

Затем откройте: http://localhost:8000
